# grub.cfg - GRUB configuration for custom kernel

# Set default boot entry
set default=0

# Set timeout for menu
set timeout=5

# Menu entry for the kernel
menuentry "My Custom Kernel" {
    # Use multiboot standard
    multiboot /boot/kernel.bin

    # Boot the kernel
    boot
}
